# 日本語を使用
export LANG=ja_JP.UTF-8

# editor
export EDITOR=vi

# config
HISTFILE=~/.history
HISTSIZE=3000
SAVEHIST=30000

# ターミナルをカラーにする
export CLICOLOR=true
export LSCOLORS=DxGxcxdxCxegedabagacad

# read local file
if [ -f $dotfiles_dir/.rc.local ]; then
    . $dotfiles_dir/.rc.local
fi
