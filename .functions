# simple find
function f() {
  find . -name "*$1*"
}

# git pull origin current
function gf() {
  git fetch --prune
}

function gp() {
  current_branch=`git rev-parse --abbrev-ref HEAD`
  git pull origin $current_branch
}
